package kernel

// CloneStateMap copies state maps so diagnostics are stable after mutation.
func CloneStateMap(in map[string]string) map[string]string {
	if len(in) == 0 {
		return nil
	}
	out := make(map[string]string, len(in))
	for k, v := range in {
		out[k] = v
	}
	return out
}
